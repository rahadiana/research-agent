<% var title = result.query.topic; %>

<!-- Header -->
<div class="flex flex-wrap items-start justify-between gap-4 mb-8">
  <div class="flex-1 min-w-0">
    <div class="flex items-center gap-3 mb-2">
      <a href="/" class="flex items-center justify-center w-8 h-8 rounded-xl bg-slate-800/50 text-slate-400 hover:text-slate-200 hover:bg-slate-700/50 transition-all">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/></svg>
      </a>
      <h1 class="text-2xl font-bold text-white truncate"><%= result.query.topic %></h1>
    </div>
    <div class="flex flex-wrap items-center gap-3 text-xs text-slate-500 ml-11">
      <% 
        var statusMeta = { 
          completed: { dot: 'bg-emerald-400', badge: 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' },
          failed: { dot: 'bg-red-400', badge: 'bg-red-500/10 text-red-400 border-red-500/20' },
          running: { dot: 'bg-amber-400', badge: 'bg-amber-500/10 text-amber-400 border-amber-500/20' },
          queued: { dot: 'bg-blue-400', badge: 'bg-blue-500/10 text-blue-400 border-blue-500/20' },
          cancelled: { dot: 'bg-slate-400', badge: 'bg-slate-500/10 text-slate-400 border-slate-500/20' }
        };
        var meta = statusMeta[result.status] || statusMeta.cancelled;
      %>
      <span class="status-badge <%= meta.badge %> border">
        <span class="status-dot <%= meta.dot %>"></span>
        <%= result.status.charAt(0).toUpperCase() + result.status.slice(1) %>
      </span>
      <% if (result.query.depth) { %>
        <span class="flex items-center gap-1">
          <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6V4m0 2a2 2 0 100 4m0-4a2 2 0 110 4m-6 8a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4m6 6v10m6-2a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4"/></svg>
          <%= result.query.depth %>
        </span>
      <% } %>
      <span class="flex items-center gap-1">
        <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"/></svg>
        <%= result.sources.length %> sources
      </span>
      <span class="flex items-center gap-1">
        <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/></svg>
        <%= new Date(result.createdAt).toLocaleDateString('en-GB', { day: 'numeric', month: 'short', year: 'numeric', hour: '2-digit', minute: '2-digit' }) %>
      </span>
      <% if (result.completedAt) { %>
        <span class="flex items-center gap-1">
          <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
          <%= new Date(result.completedAt).toLocaleDateString('en-GB', { day: 'numeric', month: 'short', year: 'numeric', hour: '2-digit', minute: '2-digit' }) %>
        </span>
      <% } %>
    </div>
  </div>
  <% if (result.report) { %>
    <div class="flex items-center gap-2 shrink-0">
      <a href="/research/<%= result.id %>/export/markdown" class="flex items-center gap-1.5 bg-slate-800/50 hover:bg-slate-700/50 text-slate-300 hover:text-slate-100 px-3.5 py-2 rounded-xl text-xs font-medium border border-slate-600/30 hover:border-slate-500/50 transition-all">
        <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z"/></svg>
        MD
      </a>
      <a href="/research/<%= result.id %>/export/json" class="flex items-center gap-1.5 bg-slate-800/50 hover:bg-slate-700/50 text-slate-300 hover:text-slate-100 px-3.5 py-2 rounded-xl text-xs font-medium border border-slate-600/30 hover:border-slate-500/50 transition-all">
        <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z"/></svg>
        JSON
      </a>
      <a href="/research/<%= result.id %>/export/html" class="flex items-center gap-1.5 bg-slate-800/50 hover:bg-slate-700/50 text-slate-300 hover:text-slate-100 px-3.5 py-2 rounded-xl text-xs font-medium border border-slate-600/30 hover:border-slate-500/50 transition-all">
        <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"/></svg>
        HTML
      </a>
    </div>
  <% } %>
</div>

<div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
  <!-- Main content -->
  <div class="lg:col-span-2 space-y-6">
    <!-- Progress bar (for running/queued) -->
    <% if (result.status === 'running' || result.status === 'queued') { %>
      <div class="glass-card rounded-2xl p-6">
        <div class="flex items-center justify-between mb-3">
          <span class="text-sm font-semibold text-slate-200 flex items-center gap-2">
            <span class="w-2 h-2 rounded-full bg-amber-400 animate-pulse"></span>
            <%= result.progress.phase.charAt(0).toUpperCase() + result.progress.phase.slice(1) %>
          </span>
          <span class="text-sm font-bold text-amber-400"><%= result.progress.percent %>%</span>
        </div>
        <div class="bg-slate-700/50 rounded-full h-2.5 overflow-hidden">
          <div id="progress-<%= result.id %>" class="progress-bar h-full rounded-full progress-glow-amber" style="width:<%= result.progress.percent %>%"></div>
        </div>
        <p id="progress-msg-<%= result.id %>" class="mt-3 text-xs text-slate-500"><%= result.progress.message %></p>
      </div>
    <% } %>

    <!-- Error -->
    <% if (result.status === 'failed' && result.error) { %>
      <div class="glass-card rounded-2xl p-6 border-red-500/20">
        <div class="flex items-start gap-3">
          <div class="w-10 h-10 rounded-xl bg-red-500/10 flex items-center justify-center shrink-0">
            <svg class="w-5 h-5 text-red-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
          </div>
          <div>
            <p class="text-sm font-semibold text-red-300">Research Failed</p>
            <p class="text-xs text-red-400/80 mt-1.5 leading-relaxed"><%= result.error %></p>
          </div>
        </div>
      </div>
    <% } %>

    <!-- Report -->
    <% if (result.report) { %>
      <!-- Summary -->
      <div class="glass-card rounded-2xl p-6">
        <h2 class="text-lg font-semibold mb-4 flex items-center gap-2.5">
          <span class="flex items-center justify-center w-7 h-7 rounded-lg bg-blue-500/10 text-blue-400">
            <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
          </span>
          Summary
        </h2>
        <div class="text-sm text-slate-300 leading-[1.85] max-w-none">
          <% 
            function splitParagraphs(text) {
              if (!text) return [];
              // Coba split by double newline dulu
              var byDouble = text.split(/\n\s*\n/).map(function(s) { return s.trim(); }).filter(Boolean);
              if (byDouble.length > 1) return byDouble;
              // Split by kalimat — titik internal (domain/singkatan) tidak memecah kalimat,
              // jadi teks di depan titik tidak hilang
              var sentences = text.split(/(?<=[.!?])\s+(?=[A-Z0-9"'“])/).map(function(s) { return s.trim(); }).filter(Boolean);
              if (sentences.length === 0) sentences = [text.trim()];
              // Kelompokkan 6-8 kalimat per paragraf biar gak kepotong2
              var groupSize = sentences.length > 20 ? 8 : 6;
              var paragraphs = [];
              for (var i = 0; i < sentences.length; i += groupSize) {
                var chunk = sentences.slice(i, i + groupSize).join(' ').trim();
                if (chunk) paragraphs.push(chunk);
              }
              return paragraphs.length > 0 ? paragraphs : [text.trim()];
            }
            var summaryParts = splitParagraphs(result.report.summary || '');
            summaryParts.forEach(function(paragraph) { 
          %>
            <p class="mb-3 last:mb-0"><%= paragraph %></p>
          <% }); %>
        </div>
      </div>

      <!-- Key Findings -->
      <% if (result.report.keyFindings.length > 0) { %>
        <div class="glass-card rounded-2xl p-6">
          <h2 class="text-lg font-semibold mb-4 flex items-center gap-2.5">
            <span class="flex items-center justify-center w-7 h-7 rounded-lg bg-emerald-500/10 text-emerald-400">
              <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
            </span>
            Key Findings
          </h2>
          <ul class="space-y-3">
            <% result.report.keyFindings.forEach(function(f) { %>
              <li class="flex items-start gap-3 text-sm text-slate-300">
                <span class="flex items-center justify-center w-5 h-5 rounded-full bg-emerald-500/10 text-emerald-400 shrink-0 mt-0.5">
                  <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/></svg>
                </span>
                <span class="leading-relaxed"><%= f %></span>
              </li>
            <% }); %>
          </ul>
        </div>
      <% } %>

      <!-- Sections -->
      <% if (result.report.sections.length > 0) { %>
        <div class="glass-card rounded-2xl p-6">
          <h2 class="text-lg font-semibold mb-4 flex items-center gap-2.5">
            <span class="flex items-center justify-center w-7 h-7 rounded-lg bg-violet-500/10 text-violet-400">
              <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h7"/></svg>
            </span>
            Report
          </h2>
          <div class="report-sections text-sm text-slate-300 leading-[1.85] max-w-none">
            <%- reportHtml %>
          </div>
        </div>
      <% } %>

      <!-- Conclusions -->
      <% if (result.report.conclusions.length > 0) { %>
        <div class="glass-card rounded-2xl p-6 border-blue-500/10">
          <h2 class="text-lg font-semibold mb-4 flex items-center gap-2.5">
            <span class="flex items-center justify-center w-7 h-7 rounded-lg bg-blue-500/10 text-blue-400">
              <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
            </span>
            Conclusions
          </h2>
          <ul class="space-y-3">
            <% result.report.conclusions.forEach(function(c) { %>
              <li class="flex items-start gap-3 text-sm text-slate-300">
                <span class="flex items-center justify-center w-5 h-5 rounded-full bg-blue-500/10 text-blue-400 shrink-0 mt-0.5">
                  <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                </span>
                <span class="leading-relaxed"><%= c %></span>
              </li>
            <% }); %>
          </ul>
        </div>
      <% } %>
    <% } else if (result.status === 'running' || result.status === 'queued') { %>
      <div class="glass-card rounded-2xl p-12 text-center">
        <div class="flex flex-col items-center gap-4">
          <div class="w-12 h-12 border-[3px] border-emerald-400/30 border-t-emerald-400 rounded-full animate-spin"></div>
          <div>
            <p class="text-base font-medium text-slate-200">Research in progress</p>
            <p class="text-xs text-slate-500 mt-1">This page will update automatically when complete</p>
          </div>
        </div>
      </div>
    <% } %>
  </div>

  <!-- Sidebar -->
  <div class="space-y-6">
    <!-- Meta card -->
    <div class="glass-card rounded-2xl p-5 space-y-4">
      <div class="flex items-center gap-2 text-sm font-semibold">
        <svg class="w-4 h-4 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
        Details
      </div>
      <div class="space-y-3 text-xs">
        <div class="flex items-center justify-between">
          <span class="text-slate-500">Status</span>
          <span class="status-badge <%= meta.badge %> border">
            <span class="status-dot <%= meta.dot %>"></span>
            <%= result.status.charAt(0).toUpperCase() + result.status.slice(1) %>
          </span>
        </div>
        <div class="flex items-center justify-between">
          <span class="text-slate-500">Depth</span>
          <span class="text-slate-300 font-medium capitalize"><%= result.query.depth || 'medium' %></span>
        </div>
        <div class="flex items-center justify-between">
          <span class="text-slate-500">Sources</span>
          <span class="text-slate-300 font-medium"><%= result.sources.length %></span>
        </div>
        <div class="flex items-center justify-between">
          <span class="text-slate-500">Created</span>
          <span class="text-slate-300"><%= new Date(result.createdAt).toLocaleDateString('en-GB', { day: 'numeric', month: 'short', year: 'numeric' }) %></span>
        </div>
        <% if (result.completedAt) { %>
          <div class="flex items-center justify-between">
            <span class="text-slate-500">Completed</span>
            <span class="text-slate-300"><%= new Date(result.completedAt).toLocaleDateString('en-GB', { day: 'numeric', month: 'short', year: 'numeric' }) %></span>
          </div>
        <% } %>
      </div>
    </div>

    <!-- Sources -->
    <div class="glass-card rounded-2xl overflow-hidden">
      <div class="px-5 py-3.5 border-b border-slate-700/30 flex items-center justify-between">
        <h3 class="text-sm font-semibold flex items-center gap-2">
          <svg class="w-4 h-4 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"/></svg>
          Sources
        </h3>
        <span class="text-xs font-medium text-slate-500 bg-slate-800/50 px-2 py-0.5 rounded-full"><%= result.sources.length %></span>
      </div>
      <div class="divide-y divide-slate-700/20 max-h-96 overflow-y-auto">
        <% if (result.sources.length === 0) { %>
          <div class="px-5 py-8 text-center">
            <p class="text-xs text-slate-500">No sources collected</p>
          </div>
        <% } %>
        <% result.sources.forEach(function(s) { %>
          <div class="px-5 py-3 hover:bg-slate-700/20 transition-colors duration-150">
            <a href="<%= s.url %>" target="_blank" rel="noopener" class="text-sm text-emerald-400 hover:text-emerald-300 font-medium block truncate transition-colors">
              <%= s.title || s.url %>
            </a>
            <div class="flex items-center gap-2 mt-1 text-xs text-slate-500">
              <span class="truncate flex-1"><%= s.url %></span>
              <% if (s.relevanceScore !== undefined) { %>
                <span class="shrink-0 px-1.5 py-0.5 rounded-md bg-slate-800 text-slate-400 text-[10px] font-medium"><%= Math.round(s.relevanceScore * 100) %>%</span>
              <% } %>
            </div>
          </div>
        <% }); %>
      </div>
    </div>

    <!-- Questions -->
    <% if (result.query.questions && result.query.questions.length > 0) { %>
      <div class="glass-card rounded-2xl p-5">
        <h3 class="text-sm font-semibold mb-3 flex items-center gap-2">
          <svg class="w-4 h-4 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.228 9c.549-1.165 2.03-2 3.772-2 2.21 0 4 1.343 4 3 0 1.4-1.278 2.575-3.006 2.907-.542.104-.994.54-.994 1.093m0 3h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
          Research Questions
        </h3>
        <ul class="space-y-2">
          <% result.query.questions.forEach(function(q) { %>
            <li class="text-xs text-slate-400 flex items-start gap-2.5">
              <span class="flex items-center justify-center w-4 h-4 rounded-full bg-emerald-500/10 text-emerald-400 shrink-0 mt-0.5 text-[10px] font-bold">&quest;</span>
              <span class="leading-relaxed"><%= q %></span>
            </li>
          <% }); %>
        </ul>
      </div>
    <% } %>

    <!-- Deep Dive: Sub Research -->
    <% if (result.status === 'completed' && result.report) { %>
      <div class="glass-card rounded-2xl p-5">
        <h3 class="text-sm font-semibold mb-3 flex items-center gap-2">
          <svg class="w-4 h-4 text-emerald-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"/></svg>
          Deep Dive — Sub Research
        </h3>
        <p class="text-xs text-slate-500 mb-3 leading-relaxed">Gali topik ini lebih dalam dengan sub-query. Anak riset otomatis terhubung sebagai cabang (graph).</p>
        <button id="suggest-btn" onclick="suggestDeepDive()"
                class="w-full flex items-center justify-center gap-2 bg-emerald-500/10 hover:bg-emerald-500/20 text-emerald-400 hover:text-emerald-300 rounded-xl px-4 py-2.5 text-xs font-medium border border-emerald-500/20 transition-all">
          <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"/></svg>
          Generate Suggestions (AI)
        </button>
        <div id="suggestions" class="hidden mt-3 space-y-1.5"></div>
        <div class="flex items-center gap-3 my-3">
          <div class="h-px flex-1 bg-slate-700/40"></div>
          <span class="text-[10px] uppercase tracking-wider text-slate-500">atau manual</span>
          <div class="h-px flex-1 bg-slate-700/40"></div>
        </div>
        <form id="sub-form" class="space-y-3" onsubmit="submitSubResearch(event)">
          <div>
            <label class="block text-xs font-medium text-slate-400 mb-1.5" for="sub-topic">Sub-query topic <span class="text-emerald-400">*</span></label>
            <input type="text" id="sub-topic" name="topic" required
                   class="w-full bg-slate-800/50 border border-slate-600/50 rounded-xl px-3.5 py-2.5 text-sm text-slate-100 placeholder-slate-500 focus:outline-none focus:ring-2 focus:ring-emerald-500/50 focus:border-emerald-500/50 transition"
                   placeholder="e.g. <%= result.query.topic %> — implementasi & proyek">
          </div>
          <div>
            <label class="block text-xs font-medium text-slate-400 mb-1.5" for="sub-questions">Questions <span class="text-slate-500 font-normal">(one per line)</span></label>
            <textarea id="sub-questions" name="questions" rows="2"
                      class="w-full bg-slate-800/50 border border-slate-600/50 rounded-xl px-3.5 py-2.5 text-sm text-slate-100 placeholder-slate-500 focus:outline-none focus:ring-2 focus:ring-emerald-500/50 transition"
                      placeholder="Aspek spesifik yang ingin digali..."></textarea>
          </div>
          <div class="flex gap-3">
            <div class="flex-1">
              <label class="block text-xs font-medium text-slate-400 mb-1.5" for="sub-depth">Depth</label>
              <select id="sub-depth" name="depth"
                      class="w-full bg-slate-800/50 border border-slate-600/50 rounded-xl px-3.5 py-2.5 text-sm text-slate-100 focus:outline-none focus:ring-2 focus:ring-emerald-500/50 transition">
                <option value="quick">Quick</option>
                <option value="medium" selected>Medium</option>
                <option value="deep">Deep</option>
              </select>
            </div>
            <div class="flex-1">
              <label class="block text-xs font-medium text-slate-400 mb-1.5" for="sub-sources">Max Sources</label>
              <input type="number" id="sub-sources" name="maxSources" min="1" max="50" value="5"
                     class="w-full bg-slate-800/50 border border-slate-600/50 rounded-xl px-3.5 py-2.5 text-sm text-slate-100 focus:outline-none focus:ring-2 focus:ring-emerald-500/50 transition">
            </div>
          </div>
          <button type="submit" id="sub-submit-btn"
                  class="w-full flex items-center justify-center gap-2 bg-gradient-to-r from-emerald-600 to-emerald-500 hover:from-emerald-500 hover:to-emerald-400 text-white font-semibold rounded-xl px-4 py-2.5 text-xs transition-all duration-200 disabled:opacity-50 disabled:cursor-not-allowed shadow-lg shadow-emerald-500/20">
            Start Sub-Research
          </button>
        </form>
        <div id="sub-status" class="mt-3 text-xs text-slate-400 hidden"></div>
      </div>
    <% } %>

    <!-- Branches (parent & children) -->
    <div class="glass-card rounded-2xl overflow-hidden">
      <div class="px-5 py-3.5 border-b border-slate-700/30 flex items-center justify-between">
        <h3 class="text-sm font-semibold flex items-center gap-2">
          <svg class="w-4 h-4 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-6l-2-2H5a2 2 0 00-2 2z"/></svg>
          Branches
        </h3>
        <a href="/graph#<%= result.id %>" class="text-xs font-medium text-emerald-400 hover:text-emerald-300 flex items-center gap-1 transition-colors">
          <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"/></svg>
          Lihat Graph
        </a>
      </div>
      <div class="divide-y divide-slate-700/20 max-h-72 overflow-y-auto">
        <% if (parent) { %>
          <div class="px-5 py-3 bg-blue-500/5">
            <p class="text-[10px] uppercase tracking-wider text-slate-500 mb-1">Parent</p>
            <a href="/research/<%= parent.id %>" class="text-sm text-blue-400 hover:text-blue-300 font-medium block truncate transition-colors">
              <%= parent.query.topic %>
            </a>
            <span class="text-xs text-slate-500"><%= parent.status %></span>
          </div>
        <% } %>
        <% if (children.length === 0 && !parent) { %>
          <div class="px-5 py-8 text-center">
            <p class="text-xs text-slate-500">Belum ada cabang. Gunakan Deep Dive untuk menambah sub-research.</p>
          </div>
        <% } %>
        <% children.forEach(function(c) {
          var cMeta = statusMeta[c.status] || statusMeta.cancelled;
        %>
          <div class="px-5 py-3 hover:bg-slate-700/20 transition-colors duration-150">
            <div class="flex items-center gap-2">
              <a href="/research/<%= c.id %>" class="text-sm text-emerald-400 hover:text-emerald-300 font-medium block truncate flex-1 transition-colors">
                <%= c.query.topic %>
              </a>
              <span class="status-badge <%= cMeta.badge %> border shrink-0">
                <span class="status-dot <%= cMeta.dot %>"></span>
                <%= c.status %>
              </span>
            </div>
            <div class="flex items-center gap-2 mt-1 text-xs text-slate-500">
              <span><%= c.sources.length %> sources</span>
              <span>·</span>
              <span><%= new Date(c.createdAt).toLocaleDateString('en-GB', { day: 'numeric', month: 'short', hour: '2-digit', minute: '2-digit' }) %></span>
            </div>
          </div>
        <% }); %>
      </div>
    </div>

    <!-- Actions -->
    <div class="glass-card rounded-2xl p-4 space-y-2">
      <a href="/" class="flex items-center justify-center gap-2 w-full bg-slate-700/30 hover:bg-slate-700/50 text-slate-300 hover:text-slate-100 rounded-xl px-4 py-2.5 text-sm font-medium transition-all">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/></svg>
        Back to Dashboard
      </a>
      <% if (result.status === 'failed') { %>
      <button onclick="retryResearch('<%= result.id %>')" class="flex items-center justify-center gap-2 w-full bg-amber-500/5 hover:bg-amber-500/10 text-amber-400 hover:text-amber-300 rounded-xl px-4 py-2.5 text-sm font-medium transition-all">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/></svg>
        Retry Research
      </button>
      <% } %>
      <button onclick="deleteAndBack('<%= result.id %>')" class="flex items-center justify-center gap-2 w-full bg-red-500/5 hover:bg-red-500/10 text-red-400 hover:text-red-300 rounded-xl px-4 py-2.5 text-sm font-medium transition-all">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"/></svg>
        Delete Research
      </button>
    </div>

    <!-- References -->
    <% if (result.report && result.report.references.length > 0) { %>
      <div class="glass-card rounded-2xl p-5">
        <h3 class="text-sm font-semibold mb-3 flex items-center gap-2">
          <svg class="w-4 h-4 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"/></svg>
          References
        </h3>
        <ol class="list-decimal list-inside space-y-1.5">
          <% result.report.references.forEach(function(ref) { %>
            <li class="text-xs text-slate-400 leading-relaxed py-0.5"><%= ref %></li>
          <% }); %>
        </ol>
      </div>
    <% } %>
  </div>
</div>

<script>
  socket.emit('research:subscribe', { resultId: '<%= result.id %>' });

  var childIds = [<%- JSON.stringify(children.map(function(c) { return c.id; })) %>];

  socket.on('research:progress', function(data) {
    if (data.resultId === '<%= result.id %>') {
      var bar = document.getElementById('progress-' + data.resultId);
      if (bar) bar.style.width = data.progress.percent + '%';
      var msg = document.getElementById('progress-msg-' + data.resultId);
      if (msg) msg.textContent = data.progress.message;
    }
  });

  socket.on('research:complete', function(data) {
    if (data.resultId === '<%= result.id %>') {
      setTimeout(function() { location.reload(); }, 1000);
    } else if (childIds.indexOf(data.resultId) !== -1) {
      // Anak riset selesai — refresh cabang
      setTimeout(function() { location.reload(); }, 1500);
    }
  });

  socket.on('research:error', function(data) {
    if (data.resultId === '<%= result.id %>') {
      setTimeout(function() { location.reload(); }, 1000);
    } else if (childIds.indexOf(data.resultId) !== -1) {
      setTimeout(function() { location.reload(); }, 1500);
    }
  });

  // Sub-research baru yang dibuat dari halaman ini — langsung muncul di Branches
  socket.on('research:started', function(data) {
    if (data.parentId === '<%= result.id %>') {
      setTimeout(function() { location.reload(); }, 1500);
    }
  });

  function escapeHtml(str) {
    var div = document.createElement('div');
    div.textContent = str;
    return div.innerHTML;
  }

  async function suggestDeepDive() {
    var btn = document.getElementById('suggest-btn');
    var box = document.getElementById('suggestions');
    btn.disabled = true;
    btn.classList.add('opacity-50', 'cursor-not-allowed');
    box.classList.remove('hidden');
    box.innerHTML = '<p class="text-xs text-slate-400 animate-pulse">AI sedang menganalisis report...</p>';
    try {
      var res = await fetch('/research/<%= result.id %>/suggest', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ count: 5 })
      });
      var data = await res.json();
      if (!res.ok) throw new Error(data.error || 'Failed');
      if (data.suggestions && data.suggestions.length > 0) {
        box.innerHTML = data.suggestions.map(function(s) {
          return '<button type="button" onclick="startSubFromSuggestion(this)" ' +
            'class="w-full text-left text-xs text-slate-300 bg-slate-800/50 hover:bg-emerald-500/10 hover:text-emerald-300 border border-slate-600/30 hover:border-emerald-500/30 rounded-xl px-3.5 py-2.5 transition-all duration-150 flex items-start gap-2">' +
            '<span class="text-emerald-400 shrink-0 mt-0.5">&plus;</span><span>' + escapeHtml(s) + '</span></button>';
        }).join('');
      } else {
        box.innerHTML = '<p class="text-xs text-slate-500">Tidak ada saran yang dihasilkan. Coba form manual di bawah.</p>';
      }
    } catch (err) {
      box.innerHTML = '<p class="text-xs text-red-400">Gagal generate saran: ' + escapeHtml(err.message || 'error') + '</p>';
    } finally {
      btn.disabled = false;
      btn.classList.remove('opacity-50', 'cursor-not-allowed');
    }
  }

  async function startSubFromSuggestion(btn) {
    var topic = btn.querySelector('span:last-child').textContent;
    await startSubResearch(topic, []);
  }

  async function startSubResearch(topic, questions) {
    var statusEl = document.getElementById('sub-status');
    statusEl.classList.remove('hidden');
    statusEl.textContent = 'Mengantri sub-research: "' + topic + '"...';
    try {
      var res = await fetch('/research/<%= result.id %>/sub', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          topic: topic,
          questions: questions,
          depth: document.getElementById('sub-depth') ? document.getElementById('sub-depth').value : 'medium',
          maxSources: document.getElementById('sub-sources') ? Number(document.getElementById('sub-sources').value) : 5
        })
      });
      var data = await res.json();
      if (!res.ok) throw new Error(data.error || 'Failed');
      statusEl.textContent = 'Sub-research diantrikan! Halaman akan refresh otomatis.';
      showToast('Sub-research queued', 'success');
      setTimeout(function() { location.reload(); }, 2000);
    } catch (err) {
      statusEl.textContent = 'Gagal: ' + err.message;
      showToast('Sub-research gagal: ' + err.message, 'error');
    }
  }

  async function submitSubResearch(event) {
    event.preventDefault();
    var topic = document.getElementById('sub-topic').value.trim();
    if (!topic) return;
    var questionsRaw = document.getElementById('sub-questions').value;
    var questions = questionsRaw.split('\n').map(function(q) { return q.trim(); }).filter(Boolean);
    await startSubResearch(topic, questions);
  }

  async function retryResearch(id) {
    try {
      var res = await fetch('/research/' + id + '/retry', { method: 'POST' });
      if (res.ok) {
        showToast('Retrying research...', 'info');
        setTimeout(function() { location.reload(); }, 1000);
      } else {
        var data = await res.json();
        alert(data.error || 'Failed to retry');
      }
    } catch {
      alert('Network error');
    }
  }

  async function deleteAndBack(id) {
    if (!confirm('Delete this research?')) return;
    try {
      var res = await fetch('/research/' + id, { method: 'DELETE' });
      if (res.ok) window.location.href = '/';
      else alert('Failed to delete');
    } catch {
      alert('Network error');
    }
  }
</script>
